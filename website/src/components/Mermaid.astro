---
const { code } = Astro.props;
---
<div class="mermaid-diagram-container" style="background: transparent; padding: 1rem; border-radius: 8px; width: 100%; overflow-x: auto; overflow-y: hidden;">
  <pre class="mermaid" style="background: transparent; border: none; margin: 0; min-width: 800px; display: inline-block;">
    {code}
  </pre>
</div>

<script>
  import mermaid from 'mermaid';
  
  // Initialize mermaid on client load
  document.addEventListener('DOMContentLoaded', () => {
    mermaid.initialize({ 
      startOnLoad: true, 
      theme: 'dark', 
      background: 'transparent',
      fontFamily: '"Ubuntu", sans-serif'
    });
    // Astro might dynamically swap content, so we ensure run is called.
    mermaid.run({ querySelector: '.mermaid' });
  });
</script>
